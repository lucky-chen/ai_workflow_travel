# ImplementationContract Design

`Contract/ImplementationContract` and [ArchitectureDesignContract.md](./ArchitectureDesignContract.md) share the same contract-check design structure.

At the current stage, this document reuses the module design structure, runtime flow, and llm-based check pattern defined in [ArchitectureDesignContract.md](./ArchitectureDesignContract.md). The main difference is that the checked target changes from architecture design output to implementation-stage output and its corresponding contract specification.
