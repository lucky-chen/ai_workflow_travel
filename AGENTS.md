# AGENTS.md

## 默认协作规则

- 默认使用直接、精确、少废话的表达。
- 不要使用“用一句话说”“一句话总结”“简单说”等总结式口头禅。
- 用户已经明确区分的概念层级，不要自行重新合并。
- 用户要求先改文档时，先改文档，不提前跳到代码或架构实现。
- 如果发现文档不同章节存在旧模型和新模型混用，优先指出冲突并在同一轮内修正一致性。
- 读取代码、读取文档、分析问题不需要用户额外批准。
- 文档类修改可以直接执行，除非用户明确要求先出变更方案。
- 代码或文档编辑默认做最小必要改动，除非用户要求整体重构。
- 代码修改如果范围较大或影响面不清晰，应先给出简短变更方案再实施。
- 变更方案应包含：
  - target
  - involved files
  - specific change items
  - risks or open questions
  - expected validation method
- 只有实际完成并验证的事项，才能标记为已完成。
- 如果改动过大，应拆成多个独立、可审阅、可验证的批次。
- 每一轮代码修改应尽量控制在单个批次范围内，避免把无关重构、行为修改和架构调整混在一起。

## 文档/代码风格

- 需求文档保持在产品需求层，不写实现细节、类名或运行时内部机制，除非用户明确要求。
- 技术架构文档保持在架构层，不下沉到模块内部实现细节，除非用户明确要求。
- 通用 document contract 是检查规约，不要写死项目内容。
- 文档中的术语一旦在上文确定，下文必须保持一致。
- 文档结构调整后，必须同步修正章节编号、引用关系和同章术语。
- 所有生成代码应保持可读、可维护。
- 不要把多个无关行为塞进一个过大的函数。
- 测试文件优先采用一个入口组织加多个按行为拆分的小测试。
- 新生成代码优先保证结构清晰，再考虑压缩行数。

## 目录约定

- `/Users/chen/Documents/workspace/ai/mvp` 视为仓库根目录。
- 正式项目文档默认放在 `project_layer/docs`。
- 设计类文档默认放在 `project_layer/docs/design_docs`。
- 模板与资源默认放在 `meta_layer/resources`，除非用户另行指定。
- 不要无明确要求地创建并行目录树或重复文档目录。

## 提交约定

- 未经用户明确要求，不主动创建 git commit。
- 如果用户要求提交，提交信息应准确反映本次目标和实际改动点。
- 不要把无关改动混入同一次提交。
- 不要改写、回滚或覆盖用户已有但与当前任务无关的修改。
- 如果用户要求提交，commit message 使用英文。
- Human commit 首行格式：`[Human]: <summary>`
- AI commit 首行格式：`[AI] <Model>: <summary>`
- commit 详情从第二行开始，使用以下格式：
  - `Target: <target>`
  - `Change items:`
  - `1. <item one>`
  - `2. <item two>`
- `Target:` 与 `Change items:` 必须分行。

## review 规则

- 当用户要求“检查”“review”文档或方案时，优先找冲突、缺口、不一致和旧模型残留。
- 先指出问题，再给改法；不要先给泛泛总结。
- review 时要特别检查：
  - 章节之间是否前后对齐
  - 术语是否一致
  - 新功能点是否映射到后续章节
  - 旧结构是否仍残留在后续章节
- 如果没有问题，再明确说明“未发现明显冲突”，不要假装一定有问题。
- 沟通优先给出明确结论，再补充理由或影响。
- 结论不确定时，直接说明不确定性、原因和当前判断。

## 工具使用偏好

- 搜索文件优先使用 `rg --files`。
- 搜索文本优先使用 `rg`。
- 读取文件时优先按段读取，不一次性加载超长内容，除非确有必要。
- 需要并行读取多个文件时，优先并行执行。
- 手工修改文件时，优先使用 `apply_patch`。
- 做目录移动、文件移动、重命名后，必须再次检查最终路径。
- git 权限如需长期协作，优先批准稳定前缀而不是一次性命令。
- 推荐稳定前缀：
  - `["git", "add"]`
  - `["git", "commit"]`
  - `["git", "status"]`
- 不要广泛自动批准破坏性或远端相关 git 命令，如 `git reset`、`git checkout`、`git push`，除非用户明确要求。

## 特定项目约束

- 当前项目中，`Core Functional Points` 是能力定义，不是用户场景。
- `Runtime Composition Modes` 与用户场景分开表达，不要混写在同一层级。
- `User Scenarios` 章节使用用户视角描述，不使用运行时内部视角描述。
- 如果 `Core Functional Points` 中已经定义了功能点名称，后续章节引用时必须严格使用相同名称。
- `gate`、`trace`、`contract`、`update` 等概念如果已被区分，就继续保持区分，不要自行合并。
- 如果用户要求弱化 `workflow` 概念，就不要在需求文档中重新强化它；优先使用 `scenario`、`process`、`runtime composition modes` 等已确定术语。
