# hello-service

Minimal text-validation client-server baseline.

## Run validation

```bash
npm test
```

Expected result:

- the client dispatches a validation request
- the server appends `" from server"`
- the validation script confirms the returned payload
